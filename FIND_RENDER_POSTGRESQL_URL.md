# 🔍 Find Render PostgreSQL Connection URL

## You're on the PostgreSQL Service Page

You see:
- **Service:** `sportingpostgres`
- **Service ID:** `dpg-d5ih1eshg0os738jia6g-a`

---

## ✅ Step 1: Click "Connect" Button

On the PostgreSQL service page, look for:

1. **"Connect"** button or tab (usually at the top)
2. **OR** look for tabs like: `[Overview] [Connect] [Logs] [Settings]`
3. **Click "Connect"** tab

---

## ✅ Step 2: Find Internal Database URL

In the "Connect" tab, you'll see:

### Internal Database URL (for Render services):
```
postgresql://username:password@dpg-d5ih1eshg0os738jia6g-a/sportingpostgres
```

**This is what you need!** Copy this entire string.

---

## ✅ Step 3: If You Don't See It

If you don't see the connection string:

1. **Check "Settings"** tab
2. **Look for "Database URL"** or **"Connection Info"**
3. **OR** look for **"Show Connection String"** button

---

## 📋 What You're Looking For

The connection string format:
```
postgresql://[USERNAME]:[PASSWORD]@dpg-d5ih1eshg0os738jia6g-a/sportingpostgres
```

**Replace:**
- `[USERNAME]` - Your database username (usually shown)
- `[PASSWORD]` - Your database password (click "Reveal" or "Show")

---

## 🎯 Quick Steps:

1. **Click "Connect"** tab on PostgreSQL service page
2. **Find "Internal Database URL"** section
3. **Copy the connection string**
4. **If password is hidden**, click "Reveal" or "Show" to see it

---

**Click the "Connect" tab - that's where the URL is!**
