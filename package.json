{
  "name": "sportsai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Root build started' && cd ui && npm install && npm run build && cd ../Sports_Ai/backend && npm install && npx prisma generate",
    "install": "echo 'Installing all dependencies'"
  }
}
